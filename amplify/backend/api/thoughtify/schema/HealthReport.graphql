type HealthReport @model @auth(rules: [{allow: owner}]) {
    id: ID!
    date: AWSDateTime
    cadence: JournalCadence
    report: AWSJSON
    thoughts: [Thought] @manyToMany(relationName: "HealthReportThoughts")
}