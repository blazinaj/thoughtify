type Event @model @auth(rules: [{allow: owner}]) {
    id: ID!
    name: String!
    description: String
    thoughts: [Thought] @manyToMany(relationName: "EventThoughts")
    address: String
    date: AWSDateTime
    comments: [Comment]
}