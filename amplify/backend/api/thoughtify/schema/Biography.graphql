type Biography @model @auth(rules: [{allow: owner}]) {
    id: ID!
    date: AWSDateTime
    cadence: JournalCadence
    entry: String
    thoughts: [Thought] @manyToMany(relationName: "BiographyThoughts")
}